name: CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
    - users: actions/checkout@v3

    - name: Rusn Script
    - run: echo "Pipeline is running!"
